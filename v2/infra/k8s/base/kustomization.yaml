apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ensimasso

resources:
  - 00-namespace.yaml
  - 10-configmap.yaml
  - 20-core.yaml
  - 30-delivery.yaml
  - 40-portail.yaml
  - 50-reseau.yaml
  - 60-sauvegarde.yaml

commonLabels:
  app.kubernetes.io/part-of: ensimasso

# Aucun generator de Secret, délibérément. Un secretGenerator lit un fichier
# qui finirait dans le dépôt — c'est-à-dire exactement le constat le plus grave
# de l'audit de la v1. Les Secrets sont créés hors de ce dossier : voir README.
